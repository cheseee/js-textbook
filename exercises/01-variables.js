/* =====================================================================
   УПРАЖНЕНИЯ К ГЛАВЕ 1: ПЕРЕМЕННЫЕ И ТИПЫ ДАННЫХ
   ---------------------------------------------------------------------
   Как решать:
   1. Создай копию этого файла: exercises/01-variables-MY-SOLUTION.js
   2. Реши каждую задачу (замени ??? на правильный код)
   3. Запусти в терминале: node exercises/01-variables-MY-SOLUTION.js
   4. Все 5 проверок должны вывести ✓
   5. Если ✗ — читай подсказку и пробуй снова

   ⚠️ НЕ ПОДГЛЯДЫВАЙ в ответы ниже. Пробуй сам.
   ===================================================================== */

// === ЗАДАЧА 1: Объяви переменные ===
// Объяви переменные по правилам:
//   - city — название твоего города (строка, const)
//   - year — текущий год (число, const)
//   - tasksLeft — сколько задач осталось (число, let, начни с 3)

// ??? напиши здесь

// Проверка (не меняй!)
if (typeof city === "string" && typeof year === "number" && tasksLeft === 3) {
  console.log("✓ Задача 1: переменные объявлены правильно");
} else {
  console.log("✗ Задача 1: что-то не так");
  console.log("  city =", typeof city, "| year =", typeof year, "| tasksLeft =", tasksLeft);
}

// === ЗАДАЧА 2: Шаблонная строка ===
// Создай строку greeting, используя шаблонные строки (backticks `` ` ``):
//   "Привет! Я из [город], сейчас [год] год."

// ??? напиши здесь

// Проверка
const expected2 = `Привет! Я из ${city}, сейчас ${year} год.`;
if (greeting === expected2) {
  console.log("✓ Задача 2: шаблонная строка правильная");
} else {
  console.log("✗ Задача 2: строка не совпадает");
  console.log("  Ожидал:  " + expected2);
  console.log("  Получил:  " + greeting);
}

// === ЗАДАЧА 3: Измени значение ===
// Уменьши tasksLeft на 1 (выполнил одну задачу).
// Потом создай строку status: "Осталось [число] задач."

// ??? напиши здесь

if (tasksLeft === 2) {
  console.log("✓ Задача 3: tasksLeft уменьшен до 2");
} else {
  console.log("✗ Задача 3: tasksLeft должен быть 2, а он", tasksLeft);
}

// === ЗАДАЧА 4: typeof ===
// Без запуска кода — подумай и напиши, что вернёт typeof для каждого значения.
// Потом раскомментируй проверку и запусти — посмотрим, угадаешь ли.

const a = typeof 42;
const b = typeof "hello";
const c = typeof true;
const d = typeof undefined;
const e = typeof null;         // ⚠️ тут подвох — вспомни из главы!

// Раскомментируй (убери //) когда напишешь ответы:
console.log("  typeof 42        =", a);
console.log("  typeof 'hello'   =", b);
console.log("  typeof true      =", c);
console.log("  typeof undefined =", d);
console.log("  typeof null      =", e, "← помнишь баг из главы?");

// === ЗАДАЧА 5: Преобразование типов ===
// Вот сломанный код — он выводит "52" вместо 7.
// Почини его, используя явное преобразование (Number или String),
// чтобы результат был числом 7.

const broken = "5" + 2;          // → "52" (неправильно!)
const fixed = /* ??? преврати "5" в число и сложи с 2 */;

if (fixed === 7) {
  console.log("✓ Задача 5: преобразование типов правильное, результат =", fixed);
} else {
  console.log("✗ Задача 5: fixed должен быть 7, а он", fixed, typeof fixed);
}

// === ИТОГ ===
console.log("\nЕсли все 5 задач показали ✓ — глава 1 освоена! Можешь переходить к главе 2.");
